return {
	"mfussenegger/nvim-lint",
	opts = {
		linters_by_ft = {
			markdown = { "write_good" },
			lua = { "selene" },
			vim = { "vint" },
		}
	},
	config = function()
		vim.api.nvim_create_autocmd({ "BufEnter", "BufWritePost" }, {
			callback = function()
				require("lint").try_lint()
			end,
		})
	end
}
